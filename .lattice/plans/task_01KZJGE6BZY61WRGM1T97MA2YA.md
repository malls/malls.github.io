# Plan: Generate shibuya/index.html

Fully specified — the spec is `shibuya/BRANDING.md` (complexity: low as a plan, the brief carries the how).

Generate `shibuya/index.html` + `style.css` (+ `script.js` if needed) via the `brand-page` skill, rendering §10 "Page application" exactly: night ground, canyon perspective scene (two rotateY walls, three depth layers, `.canyon-end` haze), `.street` scroll-owning near layer with readable content, neon sign stacks per the signage system, all under the root CLAUDE.md sub-site authoring contract (shibuya- prefixes, registry JS, no position: fixed, %3B-encoded @import if webfonts).

Acceptance: every item in the brief's §10 "The test" checklist passes; standalone preview works at `/shibuya/`; no horizontal scroll at 320px. Scope excludes sites.config.json mapping and root rebuild (follow-up task if wanted).
